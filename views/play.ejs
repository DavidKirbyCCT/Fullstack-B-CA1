<% let i = 0; %>
<div class="mt-3 d-flex flex-wrap justify-content-center" id="game-board">
        <% while (i < (gameConfig.tileNxN * gameConfig.tileNxN)) { %>
            <%- include('partials/gameTile', { i:i }) %>
            
            <% i++; %>
          <% } %>
</div>